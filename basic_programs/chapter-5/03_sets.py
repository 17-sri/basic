# set is collection of non-repetitive elements

e = set()   # defining empty set
# don't use s = {} for defining empty set, it will create empty dictionary
# sets are unordered
# sets are unindexed
# sets are immutable
# sets cannot contain duplicate values
# sets can have multiple data types

s = {2, 4, 3.5, "hello", True}
print(type(s)) # returns 'set'
print(s)